WANDB_LICENSE=your-trial-license-key-here
WANDB_BASE_URL=http://localhost:8080
WANDB_API_KEY=your-api-key-from-local-server
WANDB_ENTITY=your-username-or-team

# MLPerf / NeMo (only needed for MLPerf benchmark targets)
NGC_API_KEY=          # NVIDIA NGC API key — used for: docker login nvcr.io (pull NeMo image)
HF_TOKEN=             # HuggingFace token — used for: Llama 3.1 tokenizer (Path B data prep only)
