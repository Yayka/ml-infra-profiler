global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: ml_training_nodes
    static_configs:
      - targets:
          - "gpu-node-0:9100"  # EDIT: replace with actual IP or hostname
          - "gpu-node-1:9100"  # EDIT: replace with actual IP or hostname
        labels:
          cluster: ml-training
