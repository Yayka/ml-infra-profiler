# MLPerf Inference v5.0 — Llama3.1-8B dependencies
#
# NOTE: Docker is the primary runtime (see infra/docker/Dockerfile.mlperf-inference).
# This file documents the Python dependencies for reference and local development.
# Install into an isolated venv if running outside Docker:
#   python3 -m venv .venv-mlperf-inference
#   .venv-mlperf-inference/bin/pip install -r environments/requirements-mlperf-inference.txt

vllm==0.6.3
transformers==4.46.2
nltk==3.8.1
evaluate==0.4.0
rouge-score==0.1.2
accelerate==0.21.0
sentencepiece==0.2.0
absl-py==1.4.0
huggingface_hub
mlperf-loadgen
